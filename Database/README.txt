db.js: Initializes Sequelize with MySQL connection details.
models.js: Defines the models and their relationships.
sync.js: Syncs the models with the database, creating the necessary tables.

run 'node sync.js' to sync the models